//! Media control support for non-macOS platforms.
//!
//! On Linux and Windows, souvlaki can work without a winit event loop.
//! This module provides a simpler interface for those platforms.
//!
//! Note: On macOS, use the `macos_event_loop` module instead, as macOS requires
//! an AppDelegate/winit event loop running on the main thread for media controls to work.

// TODO: Implement simple souvlaki wrapper for Linux/Windows if needed.
// For now, this is a placeholder module.
